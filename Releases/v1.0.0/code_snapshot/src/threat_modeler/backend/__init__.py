"""Backend runtime state, execution engine, and coordination helpers.

Sub-modules
-----------
runtime_state   — process-level provider / settings state (no Streamlit)
run_manager     — pipeline execution engine (no Streamlit)
prompt_store    — file-backed agent prompt store (no Streamlit)
"""
