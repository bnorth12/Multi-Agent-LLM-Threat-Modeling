[pytest]
markers =
    llm_live: tests that require a live LLM provider credential and network access
    llm_live_browser: tests that require live browser automation for UI validation
    frontend_shell: React shell readiness lane for frame/navigation/HITL control scaffolding
    frontend_full: Full frontend workflow lane for end-to-end operational UI behavior
