# Unit Tests

Purpose:

- Validate deterministic logic in isolation.
- Keep tests fast and independent of model backends.

Typical targets:

- input parsing helpers
- schema and model validation utilities
- risk scoring helper functions
- data transformation helpers
